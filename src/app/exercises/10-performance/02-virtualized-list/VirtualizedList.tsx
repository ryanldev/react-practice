/*
 * TASK 3 — VirtualizedList
 *
 * Render a list of 10,000 items without freezing the browser by only rendering
 * the items currently visible in the viewport.
 *
 * Hooks: useState, useRef
 * Approach: manual windowing or a library (react-window / react-virtual)
 *
 * DOCS: https://react.dev/learn/render-and-commit
 */

export function VirtualizedList() {
  // Build your component here
  return null;
}
