/*
 * TASK 2 — Lazy Tabs
 * Build a tab interface where each tab's content is lazy-loaded.
 *
 * - Each tab panel is a separate lazy import
 * - Only load a tab's code when the user first clicks it
 * - Show a loading state (Suspense fallback) while the chunk loads
 */
export function LazyTabs() { return null; }
