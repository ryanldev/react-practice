/*
 * TASK 2 — Lazy Tabs
 *
 * Build a tab interface where each tab panel is a separate lazy-loaded chunk,
 * only fetched when the user first clicks that tab.
 *
 * API: React.lazy, Suspense
 *
 * DOCS: https://react.dev/reference/react/lazy
 */

export function LazyTabs() {
  // Build your component here
  return null;
}
