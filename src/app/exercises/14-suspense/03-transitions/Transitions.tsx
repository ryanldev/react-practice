/*
 * TASKS 3-4 — Transitions
 *
 * Task 3: Use useTransition to mark a state update as non-urgent so the UI
 * stays responsive while React works on it in the background.
 *
 * Task 4: Use useDeferredValue as an alternative — defer re-rendering a slow
 * subtree while keeping the input responsive.
 *
 * Hooks: useTransition, useDeferredValue, useState
 *
 * DOCS:
 * - useTransition: https://react.dev/reference/react/useTransition
 * - useDeferredValue: https://react.dev/reference/react/useDeferredValue
 */

export function TransitionsDemo() {
  // Build your demo here
  return null;
}
