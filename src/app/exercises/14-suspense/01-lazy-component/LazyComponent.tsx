/*
 * TASK 1 — Lazy Component
 *
 * Code-split a heavy component so its JavaScript is only loaded when first rendered.
 * Show a fallback while the chunk is loading.
 *
 * API: React.lazy, Suspense
 *
 * DOCS: https://react.dev/reference/react/lazy
 */

export function LazyComponentDemo() {
  // Build your demo here
  return null;
}
