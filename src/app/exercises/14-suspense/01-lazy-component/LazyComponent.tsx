/*
 * TASK 1 — Lazy Component
 * Code-split a heavy component using React.lazy + Suspense.
 *
 * - Import the component lazily: const Heavy = React.lazy(() => import('./Heavy'))
 * - Wrap in <Suspense fallback={<Spinner />}>
 * - The component's JS bundle is only loaded when it's first rendered
 *
 * DOCS: https://react.dev/reference/react/lazy
 */
export function LazyComponentDemo() { return null; }
