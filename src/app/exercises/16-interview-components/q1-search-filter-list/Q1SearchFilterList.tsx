/*
 * Q1 — Search & Filter List (Easy)
 *
 * Fetch 20 posts and render a filterable list. Handle loading, error, and success states.
 * Filter by title as the user types without storing a second copy of the list.
 *
 * Hooks: useEffect, useState, useMemo
 * API: https://jsonplaceholder.typicode.com/posts?_limit=20
 *
 * STRETCH: retry button on error, highlight matching text
 */

export function SearchFilterList() {
  // Build your component here
  return null;
}
