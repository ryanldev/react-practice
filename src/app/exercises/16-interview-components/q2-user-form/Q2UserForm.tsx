/*
 * Q2 — Multi-Field Form with Validation & Optimistic Submit (Medium)
 *
 * Build a profile form (firstName, lastName, email) with field-level validation
 * and optimistic UI — show the result immediately, roll back if the request fails.
 *
 * Hooks: useState
 * API: POST https://jsonplaceholder.typicode.com/users
 *
 * STRETCH: disable submit while in-flight, clear field errors on change
 */

export function UserForm() {
  // Build your component here
  return null;
}
