/*
 * TASKS 4-5 — useId & LiveRegion
 *
 * Task 4: Use useId() to generate stable IDs for associating form labels with inputs,
 * so the component can be rendered multiple times without ID collisions.
 *
 * Task 5: Build a LiveRegion component that announces dynamic updates to screen readers.
 *
 * Hooks: useId, useState
 * ARIA: aria-live="polite" / aria-live="assertive"
 *
 * DOCS:
 * - useId: https://react.dev/reference/react/useId
 * - ARIA Live Regions: https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/ARIA_Live_Regions
 */

export function LiveRegion() {
  // Build your component here
  return null;
}
