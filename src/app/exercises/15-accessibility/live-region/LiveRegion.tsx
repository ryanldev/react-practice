/*
 * TASK 4-5 — LiveRegion & useId
 * TASK 4: Use useId() to generate stable, unique IDs for form label associations
 *   - id={id} on input, htmlFor={id} on label
 *   - Avoids collisions when a component is rendered multiple times
 *
 * TASK 5: Build a <LiveRegion> for screen reader announcements
 *   - aria-live="polite" for non-urgent updates (form success)
 *   - aria-live="assertive" for urgent updates (errors)
 *
 * DOCS:
 * - useId: https://react.dev/reference/react/useId
 * - ARIA Live Regions (MDN): https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/ARIA_Live_Regions
 */
export function LiveRegion() { return null; }
