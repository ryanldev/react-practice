/*
 * TASK 3 — Focus Trap Modal
 * - When modal opens, focus moves inside it
 * - Tab key cycles through focusable elements inside the modal only
 * - Escape key closes the modal
 * - When modal closes, focus returns to the trigger element
 *
 * DOCS: https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/
 */
export function AccessibleModal() { return null; }
