/*
 * TASK 1 — Modal with Portal
 * Build a <Modal> that renders its children outside the current DOM tree.
 * - Use ReactDOM.createPortal(children, document.body)
 * - Accept an isOpen prop and an onClose callback
 * - Close on: backdrop click, Escape key
 * - Trap focus inside the modal while open (see Exercise 15)
 *
 * DOCS: https://react.dev/reference/react-dom/createPortal
 */
export function Modal() { return null; }
