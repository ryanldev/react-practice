# React Practice Repo — Claude Notes

## User Preferences

- **Do not implement components.** When setting up exercises, provide skeletons/stubs only. Ryan implements the logic himself.
- Only set up types, props, demo wrappers, and comments/hints — leave the implementation as `return null` or an empty function body.
